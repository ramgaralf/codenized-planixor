<?xml version="1.0" encoding="utf-8"?>
<!--
    Legacy backup rules (API 23-30).
    Excludes sensitive data (sync config with API keys) from auto-backup.
    Includes everything else (database, etc.).
-->
<full-backup-content>
    <exclude domain="file" path="datastore/" />
</full-backup-content>
