root = true

[*]
indent_style = space
indent_size = 4
end_of_line = crlf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

[*.{json,yml,yaml}]
indent_size = 2

[*.md]
trim_trailing_whitespace = false

[*.cs]
# SA1313: Record positional parameters use PascalCase intentionally (they define public properties for JSON serialization)
dotnet_diagnostic.SA1313.severity = none

# SA1010: False positive with C# 12+ collection expressions — StyleCop does not yet support the [] syntax
dotnet_diagnostic.SA1010.severity = none

# SA1009: False positive with C# 12+ collection expressions (closing paren before [])
dotnet_diagnostic.SA1009.severity = none

# SA1000: False positive with target-typed new() expressions in modern C#
dotnet_diagnostic.SA1000.severity = none

# AD0001: StyleCop SA1201 analyzer crashes on record declarations (known bug in StyleCop 1.1.x)
dotnet_diagnostic.SA1201.severity = none

# NU1903: Vulnerable NuGet packages — addressed separately in dependency update task
dotnet_diagnostic.NU1903.severity = none

# EF Core auto-generated migration files — do not edit manually
[**/Migrations/*.cs]
dotnet_diagnostic.SA1633.severity = none
dotnet_diagnostic.SA1200.severity = none
dotnet_diagnostic.SA1413.severity = none
dotnet_diagnostic.SA1400.severity = none

# Unit test files — relaxed documentation and formatting rules
[**/UnitTest.*/**/*.cs]
dotnet_diagnostic.SA1611.severity = none
dotnet_diagnostic.SA1615.severity = none
dotnet_diagnostic.SA1117.severity = none

# One type per file, and the file named after it.
#
# These are on, but they are a second net, not the first one. StyleCop 1.1.118 predates C# 10 and its syntax walk
# does not descend into a FILE-SCOPED namespace, so on `namespace X;` — which is nearly every file here — both rules
# see an empty compilation unit and report nothing. Verified by probe: the same file with a block namespace fires
# both immediately.
#
# What actually enforces the rule is OneTypePerFileTests, which parses every source file with Roslyn and also covers
# nested types — something SA1402 never inspects at all.
dotnet_diagnostic.SA1402.severity = warning
dotnet_diagnostic.SA1649.severity = warning

# The migrations are written by EF, not by a person: timestamped names, no copyright header, the generator's style.
# Declaring them as what they are beats switching off one rule at a time — Roslyn skips generated code by itself.
[**/Migrations/*.cs]
generated_code = true
